# Desafio SC Tecia para Devs

Aplicação web full-stack com backend e frontend para gerenciamento de empreendimentos, implementando operações CRUD completas através de API REST.

## 📋 Descrição da Solução

Esta aplicação foi desenvolvida para gerenciar empreendimentos do estado de Santa Catarina, permitindo o cadastro, listagem, edição e exclusão de informações sobre negócios locais. A solução segue as melhores práticas de desenvolvimento, utilizando padrões de design modernos, código limpo e arquitetura em camadas.

### Funcionalidades Principais

- **CRUD Completo**: Criar, ler, atualizar e deletar empreendimentos
- **Validação de Dados**: Validação tanto no frontend quanto no backend
- **Interface Moderna**: UI responsiva e intuitiva
- **API RESTful**: Endpoints bem estruturados seguindo padrões REST
- **Testes**: Cobertura de testes unitários e de integração
- **Tratamento de Erros**: Sistema robusto de tratamento de erros

### Modelo de Dados

A aplicação gerencia empreendimentos com os seguintes campos:

- **Nome do Empreendimento**: Nome do negócio (obrigatório, máx. 200 caracteres)
- **Nome do(a) Empreendedor(a)**: Nome do responsável (obrigatório, máx. 200 caracteres)
- **Município de Santa Catarina**: Cidade onde o negócio está localizado (obrigatório, máx. 100 caracteres)
- **Segmento de Atuação**: Categoria do negócio (obrigatório)
  - Tecnologia
  - Comércio
  - Indústria
  - Serviços
  - Agronegócio
- **E-mail ou Meio de Contato**: Informação de contato (obrigatório, máx. 200 caracteres)
- **Status**: Estado do empreendimento (ativo ou inativo, padrão: ativo)

## 🛠️ Tecnologias Utilizadas

### Backend

- **Node.js**: Runtime JavaScript
- **Express.js**: Framework web para Node.js
- **TypeScript**: Superset do JavaScript com tipagem estática
- **MongoDB**: Banco de dados NoSQL
- **Mongoose**: ODM (Object Document Mapper) para MongoDB
- **Jest**: Framework de testes
- **Supertest**: Biblioteca para testes de API
- **Express Validator**: Validação de dados
- **Helmet**: Segurança HTTP
- **CORS**: Controle de acesso cross-origin
- **Morgan**: Logger HTTP
- **dotenv**: Gerenciamento de variáveis de ambiente

### Frontend

- **React**: Biblioteca JavaScript para construção de interfaces
- **TypeScript**: Tipagem estática
- **Vite**: Build tool e dev server
- **React Router**: Roteamento
- **React Query**: Gerenciamento de estado do servidor e cache
- **React Hook Form**: Gerenciamento de formulários
- **Axios**: Cliente HTTP
- **Vitest**: Framework de testes
- **Testing Library**: Utilitários para testes de componentes React

### Ferramentas de Desenvolvimento

- **Git**: Controle de versão
- **npm**: Gerenciador de pacotes
- **Concurrently**: Execução paralela de scripts

## 📁 Estrutura do Projeto

```
desafioscteciaparadevs/
├── backend/                    # Aplicação backend
│   ├── src/
│   │   ├── config/            # Configurações (database, etc.)
│   │   ├── controllers/       # Controladores (lógica de requisições)
│   │   ├── dto/               # Data Transfer Objects
│   │   ├── errors/            # Classes de erro customizadas
│   │   ├── middleware/        # Middlewares (error handler, validation)
│   │   ├── models/            # Modelos Mongoose
│   │   ├── repositories/      # Camada de acesso a dados (Repository Pattern)
│   │   ├── routes/            # Definição de rotas
│   │   ├── services/          # Lógica de negócio (Service Layer)
│   │   ├── validators/        # Validadores de entrada
│   │   ├── __tests__/         # Testes
│   │   └── index.ts           # Ponto de entrada
│   ├── dist/                  # Código compilado (gerado)
│   ├── package.json
│   ├── tsconfig.json
│   ├── jest.config.js
│   └── env.example            # Exemplo de variáveis de ambiente
│
├── frontend/                   # Aplicação frontend
│   ├── src/
│   │   ├── components/        # Componentes React reutilizáveis
│   │   │   ├── __tests__/    # Testes de componentes
│   │   │   ├── Layout.tsx
│   │   │   ├── EmpreendimentoCard.tsx
│   │   │   └── ...
│   │   ├── pages/            # Páginas/views
│   │   │   ├── EmpreendimentoList.tsx
│   │   │   └── EmpreendimentoForm.tsx
│   │   ├── services/         # Serviços de API
│   │   │   └── api.ts
│   │   ├── types/            # Definições TypeScript
│   │   │   └── empreendimento.ts
│   │   ├── test/             # Configuração de testes
│   │   ├── App.tsx           # Componente principal
│   │   ├── main.tsx          # Ponto de entrada
│   │   └── index.css         # Estilos globais
│   ├── dist/                 # Build de produção (gerado)
│   ├── package.json
│   ├── tsconfig.json
│   ├── vite.config.ts
│   └── index.html
│
├── package.json               # Configuração do workspace
├── .gitignore
└── README.md                 # Este arquivo
```

### Padrões de Design Implementados

1. **Repository Pattern**: Separação da lógica de acesso a dados
2. **Service Layer**: Lógica de negócio isolada dos controladores
3. **DTO (Data Transfer Object)**: Objetos para transferência de dados
4. **Dependency Injection**: Injeção de dependências para melhor testabilidade
5. **Error Handling Middleware**: Tratamento centralizado de erros
6. **Validation Middleware**: Validação de dados reutilizável

## 🚀 Como Executar

### ⚡ Início Rápido

Se você já tem Docker instalado, a forma mais rápida de começar:

```bash
# 1. Instale as dependências
npm install
cd backend && npm install && cd ../frontend && npm install && cd ..

# 2. Configure o backend
cd backend && cp env.example .env && cd ..

# 3. Inicie o MongoDB
npm run mongodb:start

# 4. Inicie a aplicação
npm run dev
```

Acesse: http://localhost:5173

### Pré-requisitos

- Node.js (versão 18 ou superior)
- npm (versão 9 ou superior)
- Docker e Docker Compose (opcional, mas recomendado para MongoDB)
- MongoDB (versão 6 ou superior) - pode ser instalado localmente ou via Docker

### Instalação do MongoDB

Você tem três opções para executar o MongoDB:

#### Opção 1: MongoDB via Docker (Recomendado - Mais Fácil)

Esta é a forma mais simples e recomendada. O MongoDB será executado em um container Docker.

1. **Certifique-se de ter Docker instalado**:
```bash
# Verificar se Docker está instalado
npm run docker:check

# Ou verificar manualmente
docker --version
```

Se não tiver Docker instalado, siga as instruções abaixo:

**Ubuntu/Debian**:
```bash
# Atualizar pacotes
sudo apt update

# Instalar Docker e Docker Compose
sudo apt install docker.io docker-compose

# Iniciar serviço Docker
sudo systemctl start docker
sudo systemctl enable docker

# Adicionar seu usuário ao grupo docker (para não precisar de sudo)
sudo usermod -aG docker $USER

# IMPORTANTE: Faça logout e login novamente, ou execute:
newgrp docker

# Verificar instalação
docker --version
docker-compose --version
```

**macOS**: 
Baixe e instale [Docker Desktop](https://www.docker.com/products/docker-desktop)

**Windows**: 
Baixe e instale [Docker Desktop](https://www.docker.com/products/docker-desktop)

**Nota**: Versões mais recentes do Docker usam `docker compose` (com espaço) ao invés de `docker-compose` (com hífen). Os scripts do projeto detectam automaticamente qual usar.

2. **Inicie o MongoDB usando Docker Compose**:
```bash
# No diretório raiz do projeto (os scripts detectam automaticamente docker compose ou docker-compose)
npm run mongodb:start

# Ou manualmente (se preferir):
# docker compose -f docker-compose.dev.yml up -d
# ou
# docker-compose -f docker-compose.dev.yml up -d
```

Isso irá:
- Baixar a imagem do MongoDB 6 (se não estiver instalada)
- Criar um container chamado `desafio-mongodb`
- Expor a porta 27017
- Criar volumes persistentes para os dados

3. **Verifique se o MongoDB está rodando**:
```bash
docker ps
```

Você deve ver o container `desafio-mongodb` na lista.

4. **Para parar o MongoDB**:
```bash
docker-compose -f docker-compose.dev.yml down
```

5. **Para parar e remover os volumes (apaga os dados)**:
```bash
docker-compose -f docker-compose.dev.yml down -v
```

#### Opção 2: MongoDB Instalado Localmente

1. **Instale o MongoDB**:

**Linux (Ubuntu/Debian)**:
```bash
# Importe a chave pública do MongoDB
curl -fsSL https://www.mongodb.org/static/pgp/server-6.0.asc | sudo gpg -o /usr/share/keyrings/mongodb-server-6.0.gpg --dearmor

# Adicione o repositório
echo "deb [ arch=amd64,arm64 signed-by=/usr/share/keyrings/mongodb-server-6.0.gpg ] https://repo.mongodb.org/apt/ubuntu jammy/mongodb-org/6.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-6.0.list

# Atualize e instale
sudo apt-get update
sudo apt-get install -y mongodb-org
```

**macOS**:
```bash
brew tap mongodb/brew
brew install mongodb-community@6.0
```

**Windows**:
Baixe o instalador MSI do [site oficial do MongoDB](https://www.mongodb.com/try/download/community)

2. **Inicie o MongoDB**:

**Linux (systemd)**:
```bash
sudo systemctl start mongod
sudo systemctl enable mongod  # Para iniciar automaticamente no boot
```

**macOS**:
```bash
brew services start mongodb-community@6.0
```

**Windows**:
O MongoDB será iniciado como serviço automaticamente após a instalação.

3. **Verifique se está rodando**:
```bash
mongosh --eval "db.version()"
```

#### Opção 3: MongoDB Atlas (Cloud - Gratuito)

1. Acesse [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Crie uma conta gratuita
3. Crie um cluster gratuito
4. Obtenha a string de conexão
5. Use a string de conexão no arquivo `.env` do backend

### Instalação do Projeto

1. Clone o repositório:
```bash
git clone <url-do-repositorio>
cd desafioscteciaparadevs
```

2. Instale as dependências do projeto raiz:
```bash
npm install
```

3. Instale as dependências do backend:
```bash
cd backend
npm install
```

4. Instale as dependências do frontend:
```bash
cd ../frontend
npm install
```

### Configuração

1. Configure as variáveis de ambiente do backend:
```bash
cd backend
cp env.example .env
```

Edite o arquivo `.env` com suas configurações:
```env
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://localhost:27017/desafioscteciaparadevs
CORS_ORIGIN=http://localhost:5173
```

**Notas**:
- Se estiver usando MongoDB via Docker, a URI padrão `mongodb://localhost:27017/desafioscteciaparadevs` já funciona
- Se estiver usando MongoDB Atlas, substitua `MONGODB_URI` pela string de conexão fornecida
- Se o MongoDB estiver em outra porta ou host, ajuste a URI conforme necessário

### Verificar Conexão com MongoDB

Antes de iniciar a aplicação, você pode verificar se o MongoDB está acessível:

**Com Docker**:
```bash
docker exec -it desafio-mongodb mongosh --eval "db.version()"
```

**Com MongoDB local**:
```bash
mongosh --eval "db.version()"
```

**Testar conexão do Node.js**:
```bash
cd backend
node -e "const mongoose = require('mongoose'); mongoose.connect('mongodb://localhost:27017/desafioscteciaparadevs').then(() => { console.log('Conectado!'); process.exit(0); }).catch(err => { console.error('Erro:', err); process.exit(1); });"
```

### Executar em Desenvolvimento

**IMPORTANTE**: Certifique-se de que o MongoDB está rodando antes de iniciar a aplicação!

#### Passo 1: Iniciar o MongoDB

**Se estiver usando Docker** (recomendado):
```bash
# No diretório raiz do projeto
npm run mongodb:start

# Ou use o comando diretamente:
# docker compose -f docker-compose.dev.yml up -d
# ou
# docker-compose -f docker-compose.dev.yml up -d
```

**Se tiver MongoDB instalado localmente**:
```bash
# Linux
sudo systemctl start mongod

# macOS
brew services start mongodb-community@6.0

# Windows - O serviço já deve estar rodando
```

#### Passo 2: Iniciar a Aplicação

Você tem duas opções:

##### Opção 1: Executar backend e frontend separadamente

**Terminal 1 - Backend**:
```bash
cd backend
npm run dev
```

**Terminal 2 - Frontend**:
```bash
cd frontend
npm run dev
```

##### Opção 2: Executar ambos simultaneamente (recomendado)

No diretório raiz:
```bash
npm run dev
```

Isso iniciará tanto o backend quanto o frontend simultaneamente.

##### Opção 3: Executar tudo (MongoDB + Backend + Frontend) de uma vez

Se estiver usando MongoDB via Docker, você pode iniciar tudo com um único comando:
```bash
npm run dev:full
```

Este comando irá:
1. Iniciar o MongoDB via Docker
2. Aguardar alguns segundos
3. Iniciar o backend e frontend

#### Passo 3: Acessar a Aplicação

A aplicação estará disponível em:
- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3000
- **Health Check**: http://localhost:3000/health
- **MongoDB**: mongodb://localhost:27017

### Gerenciar MongoDB

#### Comandos Úteis para MongoDB com Docker

Você pode usar os scripts npm ou comandos Docker diretamente:

**Usando scripts npm** (mais fácil):
```bash
# Iniciar MongoDB
npm run mongodb:start

# Parar MongoDB
npm run mongodb:stop

# Ver logs do MongoDB
npm run mongodb:logs

# Acessar shell do MongoDB
npm run mongodb:shell

# Ver status do container
npm run mongodb:status
```

**Usando comandos Docker diretamente**:
```bash
# Iniciar MongoDB (use 'docker compose' ou 'docker-compose' dependendo da sua versão)
docker compose -f docker-compose.dev.yml up -d
# ou
docker-compose -f docker-compose.dev.yml up -d

# Parar MongoDB
docker compose -f docker-compose.dev.yml down
# ou
docker-compose -f docker-compose.dev.yml down

# Ver logs do MongoDB
docker logs desafio-mongodb

# Acessar shell do MongoDB
docker exec -it desafio-mongodb mongosh

# Ver status do container
docker ps | grep desafio-mongodb

# Remover tudo (incluindo dados)
docker compose -f docker-compose.dev.yml down -v
# ou
docker-compose -f docker-compose.dev.yml down -v
```

#### Comandos Úteis para MongoDB Local

**Iniciar**:
```bash
# Linux
sudo systemctl start mongod

# macOS
brew services start mongodb-community@6.0
```

**Parar**:
```bash
# Linux
sudo systemctl stop mongod

# macOS
brew services stop mongodb-community@6.0
```

**Acessar shell**:
```bash
mongosh
```

**Ver status**:
```bash
# Linux
sudo systemctl status mongod

# macOS
brew services list | grep mongodb
```

### Troubleshooting - Problemas Comuns

#### Erro "docker-compose: not found" ou "docker compose: not found"

**Problema**: Docker ou Docker Compose não está instalado

**Soluções**:

1. **Verifique se Docker está instalado**:
```bash
npm run docker:check
```

2. **Instale Docker no Ubuntu/Debian**:
```bash
# Atualizar pacotes
sudo apt update

# Instalar Docker e Docker Compose
sudo apt install docker.io docker-compose

# Iniciar serviço Docker
sudo systemctl start docker
sudo systemctl enable docker

# Adicionar seu usuário ao grupo docker
sudo usermod -aG docker $USER

# IMPORTANTE: Faça logout e login novamente, ou execute:
newgrp docker

# Verificar instalação
docker --version
```

3. **Se usar versão nova do Docker** (que usa `docker compose` ao invés de `docker-compose`):
   - Os scripts do projeto (`npm run mongodb:start`) detectam automaticamente qual comando usar
   - Você não precisa se preocupar com isso, os scripts fazem isso automaticamente

4. **Verificar permissões**:
```bash
# Se receber erro de permissão, adicione seu usuário ao grupo docker:
sudo usermod -aG docker $USER
newgrp docker
```

#### MongoDB não conecta

**Problema**: Erro de conexão ao MongoDB

**Soluções**:

1. **Verifique se o MongoDB está rodando**:
```bash
# Com Docker
npm run mongodb:status

# Localmente
# Linux
sudo systemctl status mongod

# macOS
brew services list | grep mongodb
```

2. **Verifique a porta 27017**:
```bash
# Ver se a porta está em uso
lsof -i :27017  # macOS/Linux
netstat -ano | findstr :27017  # Windows
```

3. **Verifique a URI de conexão no `.env`**:
   - Deve ser: `mongodb://localhost:27017/desafioscteciaparadevs`
   - Se usar Docker, não precisa mudar nada
   - Se usar MongoDB Atlas, use a string de conexão fornecida

4. **Reinicie o MongoDB**:
```bash
# Docker
npm run mongodb:stop
npm run mongodb:start

# Local
# Linux
sudo systemctl restart mongod

# macOS
brew services restart mongodb-community@6.0
```

#### Docker não está instalado

**Solução**: Instale Docker seguindo as instruções:
- **Linux**: `sudo apt-get update && sudo apt-get install docker.io docker-compose`
- **macOS/Windows**: Baixe [Docker Desktop](https://www.docker.com/products/docker-desktop)

#### Porta 27017 já está em uso

**Problema**: Outro MongoDB ou aplicação está usando a porta 27017

**Soluções**:

1. **Pare o MongoDB existente**:
```bash
# Se for outro container Docker
docker stop $(docker ps -q --filter "publish=27017")

# Se for MongoDB local
# Linux
sudo systemctl stop mongod

# macOS
brew services stop mongodb-community@6.0
```

2. **Ou use outra porta** (edite `docker-compose.dev.yml`):
```yaml
ports:
  - "27018:27017"  # Use 27018 no host
```
E atualize o `.env`:
```env
MONGODB_URI=mongodb://localhost:27018/desafioscteciaparadevs
```

#### Erro "Cannot connect to MongoDB"

**Soluções**:

1. **Aguarde alguns segundos** após iniciar o MongoDB (ele precisa de tempo para inicializar)

2. **Verifique os logs**:
```bash
npm run mongodb:logs
```

3. **Teste a conexão manualmente**:
```bash
# Docker
docker exec -it desafio-mongodb mongosh --eval "db.version()"

# Local
mongosh --eval "db.version()"
```

#### Dados não persistem

**Problema**: Dados são perdidos ao reiniciar o MongoDB

**Solução**: Certifique-se de que os volumes do Docker estão configurados corretamente. O `docker-compose.dev.yml` já inclui volumes persistentes.

**Verificar volumes**:
```bash
docker volume ls | grep desafio
```

### Executar Testes

#### Testes do Backend
```bash
cd backend
npm test
```

Para executar com cobertura:
```bash
cd backend
npm run test:coverage
```

#### Testes do Frontend
```bash
cd frontend
npm test
```

#### Executar todos os testes
```bash
npm test
```

## 🏗️ Como Buildar

### Build do Backend

```bash
cd backend
npm run build
```

O código TypeScript será compilado para JavaScript na pasta `backend/dist/`.

### Build do Frontend

```bash
cd frontend
npm run build
```

Os arquivos otimizados para produção serão gerados na pasta `frontend/dist/`.

### Build Completo

No diretório raiz:
```bash
npm run build
```

Isso irá buildar tanto o backend quanto o frontend.

## 📦 Como Publicar/Deploy

### Opção 1: Deploy Manual

#### Backend

1. Build do backend:
```bash
cd backend
npm run build
```

2. Configure as variáveis de ambiente de produção no servidor:
```env
NODE_ENV=production
PORT=3000
MONGODB_URI=mongodb://sua-uri-de-producao
CORS_ORIGIN=https://seu-frontend.com
```

3. Execute o backend:
```bash
npm start
```

**Recomendação**: Use um process manager como PM2:
```bash
npm install -g pm2
pm2 start dist/index.js --name backend
pm2 save
pm2 startup
```

#### Frontend

1. Build do frontend:
```bash
cd frontend
npm run build
```

2. Os arquivos em `frontend/dist/` podem ser servidos por:
   - **Nginx**: Configure um servidor web para servir os arquivos estáticos
   - **Apache**: Similar ao Nginx
   - **Vercel/Netlify**: Deploy direto via CLI ou interface web
   - **AWS S3 + CloudFront**: Para hospedagem estática

**Exemplo de configuração Nginx**:
```nginx
server {
    listen 80;
    server_name seu-dominio.com;
    root /caminho/para/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### Opção 2: Deploy com Docker (Recomendado)

#### Criar Dockerfile para Backend

Crie `backend/Dockerfile`:
```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

EXPOSE 3000

CMD ["node", "dist/index.js"]
```

#### Criar Dockerfile para Frontend

Crie `frontend/Dockerfile`:
```dockerfile
FROM node:18-alpine as build

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### Criar docker-compose.yml

No diretório raiz:
```yaml
version: '3.8'

services:
  mongodb:
    image: mongo:6
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: desafioscteciaparadevs

  backend:
    build: ./backend
    container_name: backend
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      MONGODB_URI: mongodb://mongodb:27017/desafioscteciaparadevs
      CORS_ORIGIN: http://localhost
    depends_on:
      - mongodb

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  mongodb_data:
```

#### Deploy com Docker

```bash
docker-compose up -d
```

### Opção 3: Deploy em Plataformas Cloud

#### Vercel (Frontend) + Railway/Render (Backend)

**Frontend no Vercel**:
```bash
cd frontend
npm install -g vercel
vercel
```

**Backend no Railway/Render**:
- Conecte seu repositório
- Configure as variáveis de ambiente
- O deploy será automático

#### Heroku

**Backend**:
```bash
cd backend
heroku create seu-backend
heroku config:set MONGODB_URI=sua-uri
git push heroku main
```

**Frontend**:
```bash
cd frontend
heroku create seu-frontend --buildpack https://github.com/mars/create-react-app-buildpack.git
git push heroku main
```

### Variáveis de Ambiente de Produção

Certifique-se de configurar:

**Backend**:
- `NODE_ENV=production`
- `MONGODB_URI`: URI de conexão do MongoDB de produção
- `CORS_ORIGIN`: URL do frontend em produção
- `PORT`: Porta do servidor (geralmente definida pela plataforma)

**Frontend**:
- Configure a URL da API no arquivo `src/services/api.ts` ou use variáveis de ambiente

## 📝 Endpoints da API

### Base URL
```
http://localhost:3000/api
```

### Endpoints

#### GET /empreendimentos
Lista todos os empreendimentos.

**Resposta**:
```json
{
  "success": true,
  "data": [...],
  "count": 10
}
```

#### GET /empreendimentos/:id
Retorna um empreendimento específico.

**Resposta**:
```json
{
  "success": true,
  "data": {
    "_id": "...",
    "nomeEmpreendimento": "...",
    ...
  }
}
```

#### POST /empreendimentos
Cria um novo empreendimento.

**Body**:
```json
{
  "nomeEmpreendimento": "Tech Solutions",
  "nomeEmpreendedor": "João Silva",
  "municipio": "Florianópolis",
  "segmento": "Tecnologia",
  "emailContato": "joao@tech.com",
  "status": "ativo"
}
```

#### PUT /empreendimentos/:id
Atualiza um empreendimento existente.

**Body**: (campos opcionais)
```json
{
  "nomeEmpreendimento": "Tech Solutions Updated",
  "status": "inativo"
}
```

#### DELETE /empreendimentos/:id
Deleta um empreendimento.

**Resposta**:
```json
{
  "success": true,
  "message": "Empreendimento deletado com sucesso"
}
```

## 🧪 Testes

### Estrutura de Testes

- **Backend**: Testes de integração para todos os endpoints da API
- **Frontend**: Testes de componentes React

### Executar Testes

```bash
# Backend
cd backend && npm test

# Frontend
cd frontend && npm test

# Todos
npm test
```

## 🔒 Segurança

- **Helmet**: Headers de segurança HTTP
- **CORS**: Configurado para permitir apenas origens específicas
- **Validação**: Validação de entrada em todas as rotas
- **Sanitização**: Dados sanitizados antes de serem salvos

## 📄 Licença

Este projeto foi desenvolvido para o Desafio SC Tecia para Devs.

## 👥 Contribuindo

1. Faça um fork do projeto
2. Crie uma branch para sua feature (`git checkout -b feature/AmazingFeature`)
3. Commit suas mudanças (`git commit -m 'Add some AmazingFeature'`)
4. Push para a branch (`git push origin feature/AmazingFeature`)
5. Abra um Pull Request

## 📞 Suporte

Para questões ou problemas, abra uma issue no repositório.

---

Desenvolvido com ❤️ seguindo as melhores práticas de desenvolvimento de software.
